# Iris Dataset Clustering with K-Means Algorithm

This repository contains an implementation of the **K-Means Clustering Algorithm** applied to the classic **Iris dataset** using Python and `scikit-learn`.
